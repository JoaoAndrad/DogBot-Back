// Controller stub for messages
const express = require("express");
const router = express.Router();

// TODO: implement message endpoints
router.post("/", (req, res) => res.json({ ok: true }));

module.exports = router;
